#include "stdafx.h"

void utilities::Inform(string info) const
{
	//program output runs through this function
	//currently set to cout...but could be run through a file, etc.
	cout << info << endl;
}

